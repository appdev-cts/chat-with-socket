// const { geocode, reverseGeocode } = require('../utils/distance');

// async function main() {
//   const location = 'San Francisco, USA';
//   try {
//     const coordinates = await geocode(location);
//     console.log('Geocoded Coordinates:', coordinates);

//     const reverseGeocodedLocation = await reverseGeocode(coordinates.lat, coordinates.lon);
//     console.log('Reverse Geocoded Location:', reverseGeocodedLocation);

//     const destinationCoordinates = await geocode('Los Angeles, USA');
//     const distance = model.calculateHaversineDistance(
//       coordinates.
//       coordinatesgeocode.lon,
//       destinationCoordinates.lat,
//       destinationCoordinates.lon
//     );

//     console.log(`Distance between locations: ${distance} km`);
//   } catch (error) {
//     console.error('Main error:', error);
//   }
// }



